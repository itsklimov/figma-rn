/**
 * Backward-compatible export surface for semantic naming.
 * Canonical implementation lives in core/extraction/content-pattern.
 */

export {
  detectContentPattern,
  isGenericFigmaName,
} from '../extraction/content-pattern.js';
